import { StyleSheet } from "react-native";
import { margins } from "../../globalStyles/constants";

export const footerStyles = StyleSheet.create({
  content: {
    marginVertical: margins.deafaultMargins,
    zIndex: 0,
  },
});
